---
description: 使用 Graphiti MCP 构建长期记忆。任务前先搜索、任务后自动写入 Neo4j。
alwaysApply: false
---

# Graphiti 长期记忆规范

**Graphiti 是 AI 的「记忆系统」**（知识图谱）：用于存储偏好/程序/需求/事实，**并记录做过的事情**（任务摘要、变更、结论），供后续检索与一致执行。

进行任何任务时，将 **Graphiti MCP** 作为 AI 的长期记忆（Neo4j 知识图谱）。

## ⚠️ 每次任务开始必须先用 Graphiti 搜索（必做，不得跳过）

**主 Agent 在做任何事之前，必须先执行 Graphiti 检索，不得跳过。** 这是 Vernify 项目的硬性约定：做事前先查、做事后必记。

## group_id 约定（必遵）

- **统一使用** `group_id: vernify`（或 `group_ids: ["vernify"]`）
- **不使用** `vernify-project`；该组已删除，全部记忆写入 `vernify` 组

## 前置：Context7 与 Graphiti MCP

- **Context7 查询（若可用）**：使用 Graphiti MCP 前，先通过 **Context7** 查询 Graphiti MCP API 或 Neo4j 文档；若 Context7 无对应库，则参考官方文档（[Graphiti](https://help.getzep.com/graphiti/)、[Neo4j](https://neo4j.com/docs/)）。
- **必选工具**：Graphiti MCP（见 `~/.cursor/mcp.json`，后端可为 Neo4j）；**Cursor 内服务器名**：`user-graphiti`；**URL 无尾斜杠**：`http://localhost:19283/mcp`
- **用途**：存储与检索偏好（Preference）、程序（Procedure）、事实（Fact）、事件（Episode）
- **写入 API**：本 MCP 仅暴露 **add_memory**，必填 **name** + **episode_body**（无 content/category）；可选 group_id、source。详见 `docs/GRAPHITI-MCP-REFERENCE.md`。
- **原则**：知识图谱即记忆，先搜后做、做完即存
- **硬性要求**：**每次做事之前和做事之后都要使用 Graphiti 进行查询和记录**（做事前先查、做事后必记，不可遗漏）

## 任务开始时：先 Graphiti、必要时 Serena、必要时读项目文档

每次任务/对话开始时，主 Agent 必须**按序**执行（与 `task-priority-workflow.mdc`、`agent-orchestration.mdc` 一致）：

1. **Graphiti 检索（必做，不得跳过）**：**主 Agent 必须委托 graphiti-memory SubAgent 执行**，不得自行调用 Graphiti MCP 或让用户手动执行 `/graphiti-memory`；由 graphiti-memory 使用 `search_nodes`、`search_memory_facts` 搜索与当前任务相关的偏好、程序、事实；**检索时须包含「当前开发阶段」或「当前任务阶段」**，主 Agent 据此恢复当前环节与下一步；主 Agent 遵循检索到的内容执行。阶段推进时（每完成或用户确认一个阶段），须委托 graphiti-memory **更新** Graphiti 中的当前阶段与下一步。
2. **必要时 Serena**：若涉及代码分析、查找、重构，委托 code-navigator（使用 Serena）。
3. **必要时读项目文档**：若上下文不足，先读 `docs/`（如 docs/architecture/ARCHITECTURE.md、docs/architecture/PRD.md）、`.cursor/` 下相关规则或 README。

## 开始任何任务之前（Graphiti 部分）

- **始终先搜索**：使用 `search_nodes` 查找与当前任务相关的**偏好**、**程序**、**需求**；按实体类型过滤（`Preference`、`Procedure`、**`Requirement`**）以缩小范围。
- **检索当前开发阶段**：检索时须包含「当前开发阶段」或「当前任务阶段」，主 Agent 据此恢复当前环节与下一步；阶段推进时须委托 graphiti-memory 更新 Graphiti 中的当前阶段与下一步。
- **同时搜索事实**：使用 `search_memory_facts` 发现可能与任务相关的**关系和事实**。（说明：部分外部文档使用 `search_facts`，在 Vernify 中对应 Graphiti MCP 的 `search_memory_facts`。）
- **审查所有匹配**：仔细查看匹配到的偏好、程序、需求与事实，并在后续执行中遵循。

## 工作过程中

- **遵循已发现的偏好**：使行为与检索到的偏好一致。
- **严格按程序执行**：若找到适用于当前任务的程序，按步骤执行。
- **应用相关事实**：用事实信息指导决策与建议。
- **保持一致性**：与既有偏好、程序、事实保持一致。

## 记忆来源：规范文件与 Graphiti（单一数据源）

- **不读取 `.cursor/memories/`**：Vernify 已弃用该目录，记忆仅来自规范文件与 Graphiti。
- **写入 Graphiti 的记忆必须且仅可来源于以下规范文件**，以保证记录与文档一致、避免错误或过时信息：

| 文件 | 用途 |
| --- | --- |
| `.cursor/rules/project-structure.mdc` | 项目结构、技术栈、目录、核心规则、Docker、命令 |
| `docs/architecture/ARCHITECTURE.md` | 架构、服务拓扑、数据流、模块划分（仅稳定结构；操作性细节/踩坑/决策原因写 Graphiti episode，不写此文件） |
| `docs/architecture/TECH-STACK-ANALYSIS.md` | 技术栈与数据流（合并后），与 ARCHITECTURE 互补 |
| `docs/architecture/PRD.md` | 产品需求文档 |
| `docs/architecture/MODULARIZATION.md` | 模块化设计 |
| `docs/migration/CONTENT-SOURCE-MIGRATION.md` | 内容来源：课时来自 DB，无 content 目录 |
| `.cursor/rules/refactor-convention.mdc` | 重构含义（简化、主干、不堆叠） |
| `.cursor/rules/agent-orchestration.mdc` | 主 Agent 编排、SubAgent 分工、任务前后必做 |

**不得**从其他文档、对话或推测中写入与以上文件不一致的记忆。若有 `MIGRATION.md` 等变更摘要，仅提炼与上述规范文件**已修正**的部分，不新增与规范文件冲突的节点。

## 更新 Graphiti 时：按规范文件整体重构记忆（必遵）

- **核心约定**：每次更新 Graphiti 时，**仅读取上述规范文件**，理解**整体重构**记忆（删除/修正错误、使全部记忆与规范文件一致），而**不只是**追加新说法。重构含义见 `refactor-convention.mdc`：**简化结构、突出主干、移除冗余**，不通过添加补充节点堆叠信息。
- **不得仅追加**：不得仅追加新记忆，而忽略既有错误、过时或与文档不一致的记忆。
- **按当前事实修正**：若发现既有记忆与当前事实或项目文档不符（错误、过时、被推翻），必须先**修正错误记忆**，再写入正确记忆：
  1. **删除或改写**：使用 `delete_entity_edge` 删除错误事实（entity edge），或 `delete_episode` 删除错误事件；必要时在 `add_memory` 中明确说明「替代/修正先前错误」。
  2. **再写入正确记忆**：用 `add_memory` 写入正确内容（本 MCP 无 add_episode）。
- **检索后比对**：写入前先用 `search_nodes`、`search_memory_facts` 检索相关既有记忆，若与当前事实冲突，优先修正再写入。

## 始终保存新的或更新的信息

- **记录做过的事**：完成任务后**必须**将本次完成的事项写入 Graphiti（任务摘要、变更、结论）；不可遗漏。包括但不限于：删除/创建目录或文件、配置变更、代码修改、执行命令、重要结论等。
- **立即捕获需求与偏好**：当用户表达需求或偏好时，**立即**用 `add_memory` 存储（本 MCP 无 add_episode）；长需求可拆成较短逻辑块。
- **明确标识更新**：若为对现有知识的更新，在写入时说明；若为修正错误记忆，按上节流程先删/改再写入。
- **记录程序**：当发现用户希望「如何完成某类操作」时，用 `add_memory` 记录为 **Procedure**，并标注清晰类别。
- **记录事实关系**：了解到实体之间的连接时，存储为事实。
- **完成任务后自动写入**：在完成用户交付的任务后，**必须主动使用 Graphiti 将本次完成的事项写入**，不可遗漏。
- **与 post-task-workflow 配合**：完成有实际变更的任务后，除 Graphiti 写入外，**必须**执行 `post-task-workflow.mdc` 定义的两条流程（Graphiti 检查 → Serena 分析重构 → 更新文档 → 同步 Rules/Skills/SubAgents/Commands → Graphiti 记录）。
  - 用 `add_memory` 记录本次操作摘要或任务片段（做了什么、关键结果；可复用步骤记为 **Procedure**、重要结论记为 **Fact**；本 MCP 无 add_episode），便于后续检索与一致执行。
- **用户明确要求**：AI 所做之事均须自动记录到 Graphiti，事后补记若被用户提醒则视为未遵守本规则。

## 最佳实践

- **建议前先搜索**：提出建议前先检查是否已有既定知识。
- **节点与事实结合**：复杂任务同时使用 `search_nodes` 与 `search_memory_facts` 构建完整图景。
- **使用 center_node_uuid**：探索相关信息时，可围绕特定节点做定向搜索。
- **具体优先**：更具体的信息优先于一般信息。
- **主动识别模式**：发现用户行为模式时，考虑存储为偏好或程序。

## 与 Subagent / Command 的配合

- **主 Agent 硬性约定**：执行 Graphiti 检索或写入时**必须委托 graphiti-memory SubAgent**，不得自行调用 Graphiti MCP，**不得让用户手动执行** `/graphiti-memory` 或「你去跑一下命令」。

| 场景 | 委托/触发 | 说明 |
| --- | --- | --- |
| 记忆检索、写入、复杂图谱操作 | **graphiti-memory** 子代理 | 主 Agent 须委托；SubAgent 使用 Graphiti MCP 工具 |
| 快速存一份本次任务摘要 | 委托 **graphiti-memory** | 主 Agent 不得让用户执行 `/graphiti-memory`；见 `commands/graphiti-memory.md` |
| 任务完成后完整收尾流程 | **post-task-reviewer** 或 `/post-task-review` | Graphiti 检查 → Serena 分析 → 重构 → 文档 → 知识同步；见 `post-task-workflow.mdc` |

## 参考

- `refactor-convention.mdc` - 重构含义约定（适用于 Graphiti 图谱、文档、代码等）
- **Graphiti MCP README**：[README](https://github.com/getzep/graphiti/blob/main/mcp_server/README.md)
- **aivi.fyi Cursor Rules**：[介绍文章](https://www.aivi.fyi/aiagents/introduce-Graphiti-MCP-Server#cursor-rules)
- Graphiti Cursor Rules（仓库内）：[cursor_rules.md](https://github.com/getzep/graphiti/blob/main/mcp_server/docs/cursor_rules.md)
- Graphiti 文档：[入门指南](https://help.getzep.com/graphiti/getting-started/mcp-server)
- **Vernify 使用约定**：`docs/GRAPHITI-USAGE.md`
